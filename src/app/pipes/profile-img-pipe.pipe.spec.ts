import { ProfileImgPipe} from './profile-img-pipe.pipe';

describe('ProfileImgPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ProfileImgPipe();
    expect(pipe).toBeTruthy();
  });
});
